name: test workflow

on:
  push:
    branches:
      - main

jobs:
  Running_the-linux_commands:
    runs-on: ubuntu-latest
    steps:
      - name: Current directory
        run: pwd
      - name: create new file 
        run: touch Nuru
      - name: create dir
        run: mkdir uma
      - name: copy the file to dir
        run: cp Nuru /home/runner/work/Hello-world-test/Hello-world-test/uma

  deployment_job:
    runs-on: ubuntu-latest
    #needs: Running_the-linux_commands
    steps:
      - name: Deployment
        run: echo "Deploy the contianer"
      - name: deployment is completed
        run: echo "deploy the application succesfully"

  Running_the_newcommands:
    runs-on: ubuntu-latest
    needs: [deployment_job]
    steps:
      - name: test the server
        run: echo "welcome to india"
      - name: Step two
        run: echo "this another step for second job"
      - name: Display date
        run: date