name: test workflow

on:
  push:
    branches:
      - main

jobs:
  Running_the-linux_commands:
    runs-on: ubuntu-latest
    steps:
      - name: run the commnds
        run: ls -lrt
      - name: run the demo script
        run: echo "Running the demo script..."
        